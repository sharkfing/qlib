# 项目规则

## 用户脚本目录

- 用户要求新起草、生成或创建的脚本，统一存放在项目根目录下的 `myscripts/`。
- 如果用户明确指定了其他保存路径，以用户指定路径为准。
- 修改项目中已有脚本时，保持原文件位置，不迁移到 `myscripts/`。
- `myscripts/` 下的 Python 脚本在导入项目本地模块前统一加载 `myscripts/_bootstrap.py`，
  同时兼容直接运行和 `python -m`，不要在各脚本重复计算项目根目录。
